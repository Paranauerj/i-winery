export class WineInteraction {
    constructor(
        id: number,
        date: string,
        location: string,
        move: string,
        temperature: number,
        humidity: number,
        container: string,
        responsible: string,
        addedElements: Array<{name: string, quantity: number}>
    ){

    }
}
